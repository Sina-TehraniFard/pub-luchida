# TimescaleDB 接続情報（BT のヒストリカルデータ取得に使用）
# このファイルは commit してよい。値は実環境に合わせて書き換える。
# 実際の接続情報は .env に書く（.gitignore 対象）。

BACKTEST_DB_HOST=localhost
BACKTEST_DB_PORT=5432
BACKTEST_DB_NAME=tick_data
BACKTEST_DB_USER=backtest
BACKTEST_DB_PASS=change_me
